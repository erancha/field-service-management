// The product brand. The backend names the provisioned Google calendar after this exact string
// (fsm.shared.constants.BRAND), so technician-facing copy referring to that calendar must match it.
export const BRAND = 'Field Service Management'
